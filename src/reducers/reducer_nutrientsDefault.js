const initialState = {                                          //initial state of data in reducer
    nutrients: { }
}

export default function nutrientsDefault (state = initialState) {  //creating reducer with initial state by default
    return state
}