import React, { Component } from 'react';
import { withRouter } from 'react-router-dom';

class About extends Component{
    render() {
        return (
            <div>made by me)0))</div>
        );
    }
}
export default withRouter(About);
